### Check on which branch you are working on

```git
git branch -a
git checkout branch_to_rename
```

### Rename local branch

```git
git branch -m new_name
```

### Push the changes to remote

```git
git push origin :old_name new_name
```
